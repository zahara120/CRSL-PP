<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
    <style>
        a{
            text-decoration: none;
            color: black;
        }
    </style>
</head>
<body>
    <nav class="m-3 py-2 px-5">
        <div class="d-flex justify-content-between align-items-center">
            <a href="/"><img src="/logo.svg" alt="logo"></a>
            <% if (locals.user.role === 'user') { %>
                <div class="d-flex gap-4">
                    <a href="/products">Home</a>
                    <a href="/products">List product</a>
                    <a href="/orders">Orders</a>
                    <a href="/orders/history">History</a>
                </div>
            <% } %>
            <% if (locals.user.role === 'admin') { %>
                <div class="d-flex gap-4">
                    <a href="/products">home</a>
                    <a href="/products/add" class="text-success">create new product</a>
                </div>
            <% } %>
            <div class="dropdown">
                <span class="dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">Hello, <%= locals.user.username %></span>
                <ul class="dropdown-menu">
                  <li class="dropdown-item text-success">Role : <%= locals.user.role %></li>
                  <li><a class="dropdown-item" href="/profile">profile</a></li>
                  <li><a class="dropdown-item text-danger" href="/logout">logout</a></li>
                </ul>
            </div>
        </div>
    </nav>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>
</html>